Wasaya - Frontend MVP

This is a minimal React + Vite frontend to test backend API flows.

Setup:
1. cd frontend
2. npm install
3. Copy .env example if needed. Set VITE_API_BASE if backend is not at http://localhost:8000
4. npm run dev

Stack: React, Vite, React Router, Axios, Tailwind CSS
