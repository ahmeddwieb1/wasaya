services:
  backend:
    image: ahmeddwieb/elwasya:27
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=mysql+pymysql://root:rootpassword@wasaya-db.ctao6cyqsh8g.eu-west-1.rds.amazonaws.com:3306/wasaya
      - BACKEND_BASE_URL=http://localhost:8000
